apiVersion: v2
name: cert-manager-wrapper
description: A Helm wrapper chart to deploy cert-manager via ArgoCD
type: application
version: 1.0.0
appVersion: "1.14.0"

dependencies:
  - name: cert-manager
    version: v1.14.4
    repository: https://charts.jetstack.io